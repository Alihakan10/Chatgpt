name: TradingView 16-minute BUY stabilization test
on:
  push:
    paths:
      - "diagnose_tv_16min_stabilization.py"
      - ".github/workflows/diagnose_tv_16min_stabilization.yml"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install websocket-client
      - run: python diagnose_tv_16min_stabilization.py
