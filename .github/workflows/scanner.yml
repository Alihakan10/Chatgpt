name: BIST Supertrend Scanner

on:
  workflow_dispatch:

  schedule:
    - cron: "10 7-15 * * 1-5"

permissions:
  contents: write

concurrency:
  group: bist-supertrend-scanner
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest

    env:
      TEST_MODE: "false"
      TEST_SYMBOLS: "BIST:BALSU,BIST:ERCB,BIST:INVEO"
      FORCE_SCAN: "false"
      SEND_SCAN_REPORT: "false"
      SEND_TEST_TELEGRAM: "false"
      SCAN_LIMIT: "3"

    steps:
      - name: Repository checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Node paketleri
        run: npm install

      - name: TradingView indicator metadata testi
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
        run: node tv_indicator_meta.js

